<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinWallpaper"
             x:Class="XamarinWallpaper.MainPage">

    <StackLayout Orientation="Vertical">
        <Button x:Name="bt1" Text="Consumir JSON" Clicked="bt1_clickAsync" />
        <ListView x:Name="lsCategorias">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding Id}" />
                            <Label Text="{Binding Nome}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
